# ssyze.github.io

Static personal homepage for GitHub Pages.

## Structure

- `index.html`: homepage
- `assets/css/styles.css`: site styles
- `assets/img/`: brand images
- `.nojekyll`: tells GitHub Pages to serve files directly

Push this folder to a repository named `ssyze.github.io` and enable GitHub Pages from the repository root.
